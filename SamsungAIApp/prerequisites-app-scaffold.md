# App scaffold

## Activity log

- Opened the generated Android Studio project in `D:\Code\SamsungAIApp`
- Replaced the default `Greeting` screen with a `VoicePrototypeScreen`
- Added editable transcript and AI reply fields
- Added a `Start demo` button that simulates STT and AI output
- Kept the project Compose-first so STT/TTS can be wired in next
