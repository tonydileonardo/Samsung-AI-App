# JDK 17

## Activity log

- Installed Temurin JDK 17 with `winget install --id EclipseAdoptium.Temurin.17.JDK -e --source winget`
- Set `JAVA_HOME` to the Temurin install path
- Added `%JAVA_HOME%\bin` to the user `PATH`
- Verified `java -version`

## Decision

Use open-source JDK 17 for Android and Gradle development in VS Code.
