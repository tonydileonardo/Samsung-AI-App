# Git

## Activity log

- Installed Git for Windows with `winget install --id Git.Git -e --source winget`
- Verified `git --version`
- Initialized the local repository in `D:\Code`
- Configured Git identity for commits
- Added `origin` pointing to the GitHub repo
- Pushed the local history to GitHub
